apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "taskd.fullname" . }}-test-connection"
  labels:
    {{- include "taskd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "taskd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
